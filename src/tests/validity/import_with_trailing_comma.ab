import {
    replace_all,
    replace_regex,
} from "std/text"
import { math_sum, math_abs, } from "std/math"

// Output
// c
// 3

echo replace_regex(replace_all("aa", "a", "b"), "b+", "c", true)
echo math_sum([math_abs(-2), 1])
